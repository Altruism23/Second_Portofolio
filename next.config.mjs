/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "ptmdsb.com",
            "randomuser.me",
            "keenitsolutions.com"
        ]
    }
};

export default nextConfig;